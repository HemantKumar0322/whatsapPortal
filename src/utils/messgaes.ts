export const INVALID_KEY_ADDRESS = 'Invalid %key% address';
export const REQUIRED_KEY = '%key% is required';
export const KEY_MIN_LENGTH = '%key% must be at least %length% characters';
export const PASSWORD_REGEX_MESSAGE = 'Password must contain at least one letter and one number';
export const MAX_LENGTH = '%key% must be less than %length% characters';

